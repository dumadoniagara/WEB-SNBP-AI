# Backend reachable on the shared Docker network (container name + /api)
BACKEND_API_URL=http://snbp-ai-app:8080/api
# Name of the backend stack's docker network (docker network ls)
BACKEND_NETWORK=snbp-ai_default
